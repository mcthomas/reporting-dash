
</head>
<body style="background-image: linear-gradient(to right, #FFFFFF, #C0C0C0);">
<div class="container">
<table class="responsive-table highlight">
  <thead>
  <tr>
    <th>User</th>
    <th>Associate</th>
    <th>Lead</th>
    <th>Admin</th>
  </tr>
  </thead>
  <h5><u class="purple-text">Manage Users</u></h5>
  <tbody>
<% @users.each_with_index do |user, index|%>
  <tr>
    <td><span class="truncate" style="display: inline-block; width: 162px;"><%=user.email %></span></td>
    <td ><span class="truncate" style="display: inline-block; width: 162px;"><%=user.associate %></span></td>
    <td ><span class="truncate" style="display: inline-block; width: 162px;"><%=user.lead %></span></td>
    <td ><span class="truncate" style="display: inline-block; width: 162px;"><%=user.admin %></span></td>
    <td><%= link_to "edit", edit_user_path(user), style:'float: right;', class:'btn btn-small yellow darken-2' %></td>
    <td><%= button_to "delete",user_path(user), method: :delete, form: {data: {turbo_confirm: "Are you sure you want to delete this channel plan?"}}, class:"btn btn-small red" %></td>
   </tr>
  <%end %>
  </tbody>
</table>
   <br><br><br>
<%= button_to "logout", destroy_user_session_path, method: :delete, class:'btn btn-small red darken-3', style:'position: fixed; bottom: 10px; left: 10px'  %>
   </div>
   </body>
